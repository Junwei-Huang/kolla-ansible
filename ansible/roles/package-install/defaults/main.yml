---
control_node:
    group: control
    pkg_install:
      - corosync
      - pacemaker
      - pcs
      - resource-agents

all_node:
    group: baremetal 
    pkg_install:
      - nfs-utils
      - fping
      